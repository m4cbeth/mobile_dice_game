extends Node

const knights = "knights"
const slimes = "slimes"
const bad_guys = "bad_guys"
const good_guys = "good_guys"

#
#const GROUP_KEYS = ["BAD_GUYS", "GOOD_GUYS", "NEUTRAL"]
#const GROUPS = GROUP_KEYS.reduce({}, func(acc, key):
	#acc[key] = key.to_lower()
	#return acc
#)

# or even a one liner
#const GROUPS := { key: key.to_lower() for key in GROUP_KEYS }
